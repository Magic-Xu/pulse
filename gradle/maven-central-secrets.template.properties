# Copy these entries into ~/.gradle/gradle.properties (recommended)
# or set them as environment variables in CI.
#
# Values below are placeholders only.

# Sonatype Central Portal user token (not your account password)
mavenCentralUsername=TODO_REPLACE_WITH_CENTRAL_PORTAL_TOKEN_USERNAME
mavenCentralPassword=TODO_REPLACE_WITH_CENTRAL_PORTAL_TOKEN_PASSWORD

# ASCII-armored GPG private key and passphrase used for artifact signing
# Keep line breaks escaped as \n when stored in plain properties
signingInMemoryKey=TODO_REPLACE_WITH_ASCII_ARMORED_PRIVATE_KEY
signingInMemoryKeyPassword=TODO_REPLACE_WITH_GPG_KEY_PASSWORD

# Optional key id (not always required)
# signingInMemoryKeyId=TODO_OPTIONAL_KEY_ID
